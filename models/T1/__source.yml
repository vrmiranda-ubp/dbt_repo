version: 2
sources:
  - name: T1
    database: snowflake_sample_data
    schema: tpch_sf1
    tables:
      - name: customer
        description: "Raw customer data from the CRM system."
        columns:
          - name: C_CUSTKEY
            description: "Unique identifier for a customer."
            tests:
              - not_null
              - unique
          - name: C_NAME
            description: "Full name of the customer."
      - name: orders
        description: "Raw order data from the e-commerce platform."
        columns:
          - name: O_ORDERKEY
            description: "Unique identifier for an order."
            tests:
              - not_null
              - unique
      - name: supplier
        description: "Raw Supplier data from the e-commerce platform."
        columns:
          - name: S_SUPPKEY
            description: "Unique identifier for a supplier."
            tests:
              - not_null
              - unique
      - name: lineitem
        description: "Raw Lineitem data from the e-commerce platform."
        columns:
          - name: L_ORDERKEY
            description: "Unique identifier for an Lineitem."
            tests:
              - not_null
      - name: nation
        description: "Raw Lineitem data from the e-commerce platform."
        columns:
          - name: N_NATIONKEY
            description: "Unique identifier for an Lineitem."
            tests:
              - not_null              
